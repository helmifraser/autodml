import numpy as np
import pandas as pd
import keras

from keras.models import load_model

DATASET_PATH = "/media/helmi/My Passport/helmi/carla_dataset/val"

def test(data_path, callbacks=None):
    print("todo")


test(DATASET_PATH)
